# TrackLinker: 100% Бесплатный Production Plan

## Цель

Запустить продукт для всех пользователей **БЕСПЛАТНО** (для вас и для них).

---

## ✅ Полностью бесплатный стек

### Frontend + Backend

**Vercel** (Free Tier)

- ✅ Unlimited bandwidth
- ✅ 100 deployments/day
- ✅ HTTPS автоматически
- ✅ Serverless Functions (100GB-hours/месяц)
- ✅ Свой домен (можно подключить бесплатно)
- **Стоимость:** $0

### Database

**Supabase** (Free Tier)

- ✅ 500MB PostgreSQL
- ✅ 50,000 Monthly Active Users
- ✅ 2GB bandwidth
- ✅ Автоматический backup
- **Стоимость:** $0

### APIs

- **Spotify API:** Бесплатно (после quota extension)
- **YouTube API:** Бесплатно (10,000 quota units/day = ~100 плейлистов)

### Мониторинг (опционально)

- **Sentry** (Free Tier): 5k errors/месяц
- **Vercel Analytics**: Встроено бесплатно

---

## 🎯 Реалистичные лимиты бесплатного тарифа

### Supabase limits

- **500MB database** = ~50,000 пользователей (если хранить только токены)
- **2GB bandwidth** = ~20,000 requests/месяц

### Vercel limits

- **100GB-hours functions** = ~10 млн API calls (для простых операций)

### YouTube API

- **10,000 quota units/day**:
  - Создание плейлиста: 50 units
  - Добавление трека: 50 units
  - **Итого:** ~100 операций/день на пользователя

**Вывод:** Этого хватит для **тысяч активных пользователей** в месяц.

---

## 📋 План реализации (100% бесплатно)

### Шаг 1: Создать Vercel проект (5 минут)

1. Зайти на [vercel.com](https://vercel.com)
2. Sign up через GitHub (бесплатно)
3. Подключить ваш GitHub repo `TrackLinker`
4. **Готово!** Получите домен: `tracklinker.vercel.app`

### Шаг 2: Настроить Supabase (10 минут)

1. Зайти на [supabase.com](https://supabase.com)
2. Создать проект (бесплатно)
3. Создать таблицу `users`:

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE,
  spotify_access_token TEXT,
  spotify_refresh_token TEXT,
  spotify_expires_at TIMESTAMP,
  youtube_access_token TEXT,
  youtube_refresh_token TEXT,
  youtube_expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);
```

4. Скопировать `Database URL` в environment variables Vercel

### Шаг 3: Добавить Backend OAuth (1-2 дня работы)

Создать файлы в `/api` (Vercel Functions):

**`/api/auth/spotify.ts`**

```typescript
// Редирект на Spotify OAuth
export default function handler(req, res) {
  const authUrl = `https://accounts.spotify.com/authorize?client_id=${SPOTIFY_CLIENT_ID}&response_type=code&redirect_uri=${REDIRECT_URI}&scope=...`;
  res.redirect(authUrl);
}
```

**`/api/auth/spotify/callback.ts`**

```typescript
// Получить токен от Spotify, сохранить в Supabase
export default async function handler(req, res) {
  const { code } = req.query;
  const token = await exchangeCodeForToken(code);
  await saveToSupabase(token);
  res.redirect("/");
}
```

Аналогично для YouTube.

### Шаг 4: Обновить Frontend (1 день работы)

Заменить текущую логику:

**Было (BYO):**

```typescript
const spotifyProvider = new SpotifyProvider({
  clientId: userProvidedClientId, // ❌ Пользователь вводит
  redirectUri: window.location.href,
});
```

**Станет (Production):**

```typescript
// При клике "Connect Spotify"
window.location.href = "/api/auth/spotify";

// После возврата — токен уже сохранен на сервере
// Все API вызовы идут через backend:
fetch("/api/spotify/playlists");
```

### Шаг 5: Spotify Quota Extension (бесплатно, ~2 недели ожидания)

1. В [Spotify Dashboard](https://developer.spotify.com/dashboard)
2. **Quota Extension Request**
3. Описать проект: "Бесплатный инструмент для переноса плейлистов между сервисами"
4. Приложить ссылку: `https://tracklinker.vercel.app`
5. **Обычно одобряют за 1-2 недели**

### Шаг 6: Google OAuth Verification (бесплатно, если нужно)

Если YouTube API требует verification:

1. Заполнить форму в Google Cloud Console
2. Приложить Privacy Policy (можно сгенерировать через [TermsFeed](https://www.termsfeed.com/))
3. Ждать одобрения (~1-2 недели)

---

## 🚀 Альтернатива: Browser Extension (еще проще!)

Если не хочется backend — можно выпустить как **расширение для Chrome**.

### Плюсы:

- ✅ **Полностью бесплатно** (OAuth работает локально)
- ✅ Не нужен сервер
- ✅ Токены хранятся локально (больше приватности)
- ✅ Chrome Web Store бесплатно (единоразовый взнос $5)

### Минусы:

- ❌ Только для Chrome (нужно портировать на Firefox отдельно)
- ❌ Обновления через Web Store (ревью ~1-2 дня)

### Как релизнуть:

1. Адаптировать под Manifest V3
2. Упаковать: `npm run build`
3. Загрузить в [Chrome Web Store Developer Dashboard](https://chrome.google.com/webstore/devconsole)
4. Заплатить **$5 единоразово** (регистрация разработчика)
5. Публикация бесплатна, ревью ~1-2 дня

---

## 💡 Моя рекомендация для вас

### Вариант A: Vercel + Supabase (Web App)

**Лучше, если:**

- Хотите доступ с любого устройства (телефон, планшет)
- Планируете добавить социальные фичи (публичные плейлисты и т.д.)
- **Стоимость:** $0 навсегда (в рамках free tier)

### Вариант B: Chrome Extension

**Лучше, если:**

- Хотите максимально быстро запустить
- Не хотите поддерживать сервер вообще
- **Стоимость:** $5 единоразово (регистрация в Chrome Web Store)

---

## 🛠️ Я помогу реализовать

Скажите, какой путь выбираете:

1. **Web App (Vercel)** — я помогу:

   - Создать Vercel Functions для OAuth
   - Настроить Supabase
   - Рефакторить frontend
   - Задеплоить

2. **Chrome Extension** — я помогу:
   - Адаптировать под Manifest V3
   - Упаковать для публикации
   - Написать инструкцию для Chrome Web Store

Оба варианта **фактически бесплатны** (кроме $5 за Chrome extension регистрацию).

Что скажете?
